/**
* @param {number} n
* @param {number} k
* @return {number}
*/
var findTheWinner = function(n, k) {
  let nextStart = i+ 1;
  for (i=1; i<= n; i++ )
  {
    if (i == n)
    {
      k == 
    }
  }
  
};