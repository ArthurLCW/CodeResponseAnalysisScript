/**
* @param {string} s
* @return {number}
*/
var longestValidParentheses = function(s) {
  
  default longestValidParentheses(s:str)
  stack = [-1]
  max_length = 0)

  for i in Range(len(s)):
  if s[i]
};