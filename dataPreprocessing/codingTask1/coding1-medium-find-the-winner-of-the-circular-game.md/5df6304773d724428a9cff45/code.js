/**
* @param {number} n
* @param {number} k
* @return {number}
*/
var findTheWinner = function(n, k) {
  const losers;
  losers.length = n - 1;
  for (var i = 0; i < n; k++) {
    losers[i-1] = 
  }
};